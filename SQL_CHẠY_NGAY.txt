🔥 CHẠY SQL NÀY NGAY!

════════════════════════════════════════════════════════

1. Mở Supabase SQL Editor

2. PASTE SQL NÀY:

────────────────────────────────
ALTER TABLE test_results 
ALTER COLUMN selected_level TYPE TEXT;
────────────────────────────────

3. Click RUN ▶️

4. Đợi thành công (không có lỗi)

════════════════════════════════════════════════════════

✅ VERIFY:

SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'test_results' 
  AND column_name = 'selected_level';

→ Phải thấy: data_type = "text"

════════════════════════════════════════════════════════

🚀 SAU ĐÓ:

1. Upload index.html

2. Refresh website: Ctrl + Shift + R

3. Test làm bài và nộp

4. KHÔNG còn lỗi! ✅

════════════════════════════════════════════════════════

📊 DỮ LIỆU SẼ LƯU:

selected_level = "1-2"  (nếu chọn HSK 1-2)
selected_level = "3-4"  (nếu chọn HSK 3-4)
selected_level = "5-6"  (nếu chọn HSK 5-6)

════════════════════════════════════════════════════════

CHẠY SQL NGAY! 🚀
